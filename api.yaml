# CODEBASE INFO
# Last pushed to master branch: 2016-05-10T19:37:15+CEST
# Last change to master branch: Working 'search' method
# Last pushed to dev branch: ?
# Last change to dev branch: ?
#
# DEPLOYMENT INFO
# Last deployed dev version: 2016-05-10T19:24:26+CEST
# Last deployed prod version: ?
# command: gcloud preview app deploy api.yaml
# args:
#   --project vertnet-portal  # Optional, no need if gcloud is configured to use this project
#   --version prod|dev  # Indicates version to use. If 'Prod', --promote flag should be used
#   --promote  # Optional, removes the version id from the deployment URL. Skip for testing and/or dev version

module: api-module
runtime: python27
api_version: 1
threadsafe: yes

handlers:

- url: /api/*.*
  script: api.handlers
- url: /service/*.*
  script: api.handlers

libraries:
- name: webapp2
  version: "2.5.2"
# - name: jinja2
#   version: latest

skip_files:
- ^data$